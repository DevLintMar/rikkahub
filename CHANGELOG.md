# 更新日志（fork）

> 本文件只记录**本 fork 相对上游 [rikkahub/rikkahub](https://github.com/rikkahub/rikkahub) 的
> 用户可见改动**。上游自身的功能与修复见上游仓库的 release 页。内部维护信息见
> `docs/superpowers/handoffs/`。

## 未发布

### ⚠️ 升级注意（行为变化）

- **标题与建议模型固定跟随「快速模型」。** 上游 `9365c297`（close #1768）移除了单独的标题模型 /
  建议模型配置。此前显式配过这两项的用户，升级后会**静默回退到快速模型** —— 请到「设置 → 快速模型」
  确认选的是否是你想要的那个。
- **网络自动重试默认开启。** 只对 `IOException` 生效，最多 3 次、退避 1/2/4 秒（彻底失败时最多多等 7 秒）。
  可在「设置 → 网络」关闭。
- **纯附件也能发送。** `sendOnEnter` 打开时，只贴图片不打字按回车会**直接发送**（此前那颗键按下去
  没有任何反应）；同时「只打空格」不再能发出空消息。
- **搜索按钮只表示「外挂搜索」。** 模型自带服务端搜索（Claude `web_search` / Gemini `googleSearch` /
  OpenAI Responses `web_search`）时，打开「联网搜索」不会额外挂外挂搜索 —— 现在提示会明确说明，
  不再假装已生效。
- **Qwen3 TTS 旧模型已下线。** 配置里的 `qwen3-tts*` 会在读取时自动迁移到 `qwen-audio-3.0-tts-plus`。
  音色（voice）在各模型间不通用，若提示音色无效请在 TTS 设置里重选。
- **审批改为「等当前这轮生成结束」。** 生成中点审批不再打断当前轮次（上游 `55506496` 修复连续
  审批丢失）。
- **不再内置第三方赞助商供应商。** 移除 APIMart 与 MaruCode 的默认 / 推荐条目。已经添加过的不会被
  删除，但从现在起删掉之后不会再被自动补回。
- **OCR 识别不会再无限等待。** 单次调用上限 120 秒，超时即把错误交回模型（此前服务端「连得上
  但不返回」时，一次识别最坏要等 10 分钟 × 4 次重试，每张图各算一遍）。

### 修复

- **点开没有应用能打开的链接不再崩溃。** 模型写出的 `file:///data/user/0/…` 这类真机绝对路径、
  以及任何拼错的 scheme，系统里都没有处理者，此前一点就抛 `ActivityNotFoundException` 把会话
  整个崩掉。现在这类链接点了只会「没反应」（失败只记日志）；工作区内的 `file:///workspace/…`
  仍按原来的方式在应用内打开。
- **BMP 图片能发出去了。** `read_image` 与工作区文件列表都认 bmp，但发送时按文件头识别类型的
  那一步不认，BMP 一律发不出去。现在识别并统一转成 JPEG 再发给模型。
- **S3 / WebDAV 恢复前会先确认。** 此前点一下「恢复」就直接用备份覆盖数据库与设置，只有本地
  导入会问一句；现在三处一致。
- **备份 / 恢复的诊断信息补回了丢失的字段。** 恢复日志重新包含「本次要恢复哪些内容（db/文件）」、
  用户头像类型、以及图片头像数的**分母**（`0/3` 与「本来就没有图片头像」此前无法区分）；备份日志
  里 `uploadFiles` 不再把 AI 生成的 `images/` 混在一起计数。
- **「网页视图」能显示本地图片了。** 消息附件、以及文本里 `file:///workspace/…`、`file:///upload/…`
  这类图片链接此前在网页视图里全是空白 —— 预览页的 origin 是 `https://rikkahub.local`，从这个 origin
  引用 `file://` 子资源会被 WebView 直接拦掉。现在本地图片改写成应用内虚拟域名的 URL，由拦截器按
  沙箱路径读文件返回（路径解析仍走 `WorkspaceFileUrlResolver`，沙箱外的文件拿不到）。
  顺带放开预览页的混合内容策略，`http://` 图片也不再被拦。
- **按图片导出对话时，图片不再空白。** 导出走的是离屏渲染 + 固定 100ms 后截图，而图片是异步加载的，
  100ms 内基本没加载完。现在改成导出前先把图片同步加载成位图再渲染。
- **滚动对话时图片不再上下跳。** markdown 行内图加载前由占位图（1024×1024 正方形）决定高度，
  加载后才换成真实比例；消息列表回收后滚回来又重新经历一次，看起来就是高度在跳。现在把首次加载
  得到的真实宽高比记下来，此后同一进程内重新组合直接按正确比例占位。
- **偏好设置里的「网络」换成地球图标。**
- **「时间提醒间隔」从助手的「记忆」页移到「提示词」页**，紧挨时间提醒开关。
- **工具输出的落盘内容。** 超过 32KB 的工具输出写入 `/tool_outputs` 的是**完整原文**；此前顺序反了
  （先按 100KB 硬裁再落盘），模型 `cat` 也拿不到全量。
- **`/tool_outputs` 不再每次冷启清空。** 改为保留 7 天 / 总量 64MB 上限 —— 跨重启的历史会话里
  `cat /tool_outputs/xxx.txt` 不再 404。
- **工作区终端补齐 `/upload` 与 `/tool_outputs` 挂载**，与 AI 的 `workspace_shell` 一致（此前终端里
  `ls /upload` 是空的，而提示词已经向模型承诺这些路径可读）。
- **MCP 服务器名非法导致消息队列暂停时，提示里会说明这一点**，用户知道去哪里恢复。
- **Exa 搜索同时请求正文与摘录**，并支持 `max_age_hours` 时效过滤；默认路径的正文带上限，
  不会因此撑大响应体。
- **子代理的工具输出也有 100KB 上限**，并带上会话 id（多轮请求可命中同一 prompt cache）。

### 内部

- `pre` 变体改用 `matchingFallbacks`，11 个库模块不再各自声明重复的 `pre` buildType
  （上游每加一个库模块都会漏，`assemblePre` 已经因此红过一次）。
- fork 的 R8 keep 规则从上游文件里拆出，放进 `app/src/main/keepRules/fork.keep` ——
  以后再解冲突不会再连上游的 keep 修复一起吃掉。
- `.gitignore` 的 `references` 改成锚定路径，不再吞掉 `.agents/skills/*/references/`。
- `WorkspaceShellContext` 收敛到唯一构造点、字段去掉默认值，并加单测锁死字段集合
  （上游加字段时，此前会出现「编译过、只是漏传」）。
- CI 门禁脚本接进 nightly；单测从只跑 `:app` 改成全模块（其余模块约 49 个测试文件此前从未跑过）；
  新增每周一次的 instrumentation 测试工作流，以及按需重新生成 baselineProfile 的入口。
- `pre` 变体的混淆配置改用与 release 相同的 AGP 9 `optimization {}` DSL。
- 自定义 action / scheme 按变体隔离（release / debug / pre 不再抢同一 intent）。
- 补齐 4 个终端无障碍串与预览按钮文案的六语翻译。
